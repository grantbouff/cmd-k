{
  "name": "cmd-k",
  "version": "1.0.0",
  "description": "Webflow components",
  "scripts": {
    "build": "npx esbuild --bundle src/init.js --outfile=dist/home-bundle.js --minify",
    "watch": "npx esbuild --bundle src/init.js --outfile=dist/home-bundle.js --minify --watch"
  },
  "dependencies": {
    "gsap": "^3.12.7"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
